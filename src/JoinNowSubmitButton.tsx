import styles from "./JoinNowSubmitButton.module.css"

export default function JoinNowSubmitButton() {
  return (
    <div className={styles["default"]}>
      <p className={styles["join-now-label"]}>{"Join Now Label"}</p>
    </div>
  )
}
